---
import referencesData from "../data/referencias.json";
const { mediaItems } = referencesData;
---

<body>
    
    <section class="references-section">
        <div class="gold-particles" id="particles"></div>
        
        <div class="references-container">
            <h2 class="section-title">Van Gogh en la Cultura Popular</h2>
            
            <p class="intro-text">
                La figura de Vincent van Gogh ha trascendido el mundo del arte para convertirse en un icono cultural presente en numerosas películas, series y documentales. Explora cómo su legado perdura e inspira en la cultura moderna.
            </p>
            
            <div class="media-grid">
                {mediaItems.map((item) => (
                    <div class="media-card">
                        <div class="media-image">
                            <img src={item.image} alt={item.title} loading="lazy" />
                            <div class="media-type">{item.type}</div>
                            <div class="image-overlay"></div>
                        </div>
                        <div class="media-content">
                            <h3 class="media-title">{item.title}</h3>
                            <span class="media-year">{item.year}</span>
                            <p class="media-description">{item.description}</p>
                            <div class="van-gogh-reference">
                                <h4 class="reference-title">
                                    <i class={item.referenceIcon}></i> 
                                    {item.referenceTitle}
                                </h4>
                                <p class="reference-text">{item.referenceText}</p>
                            </div>
                        </div>
                    </div>
                ))}
            </div>
            
            <div class="quote-section">
                <div class="quote-decoration">✦</div>
                <p class="quote-text">"Sueño con pintar y luego pinto mis sueños"</p>
                <p class="quote-author">- Vincent van Gogh</p>
                <div class="quote-decoration">✦</div>
            </div>
        </div>
    </section>

    
    <style>
        .references-section {
            background: linear-gradient(135deg, #1a1304 0%, #2c241b 50%, #1a1304 100%);
            min-height: 100vh;
            position: relative;
            overflow: hidden;
        }
    
        .references-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 4rem 2rem;
            position: relative;
            z-index: 2;
        }
    
        .section-title {
            text-align: center;
            margin-bottom: 4rem;
            font-size: 3.5rem;
            color: #F8F0C6;
            position: relative;
            padding-bottom: 1.5rem;
            font-weight: 700;
            font-family: 'Cinzel', serif;
            text-shadow: 3px 3px 6px rgba(0, 0, 0, 0.7);
            background: linear-gradient(45deg, #D4AF37, #F4E4BC, #D4AF37);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
    
        .section-title::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 200px;
            height: 4px;
            background: linear-gradient(to right, transparent, #D4AF37, #F4E4BC, #D4AF37, transparent);
            border-radius: 3px;
            box-shadow: 0 0 15px rgba(212, 175, 55, 0.6);
        }
    
        .section-title::before {
            content: '✦';
            position: absolute;
            bottom: -15px;
            left: 50%;
            transform: translateX(-50%);
            color: #D4AF37;
            font-size: 1.8rem;
            text-shadow: 0 0 20px rgba(212, 175, 55, 0.8);
            animation: sparkle 3s ease-in-out infinite;
        }
    
        @keyframes sparkle {
            0%, 100% { transform: translateX(-50%) scale(1); opacity: 0.8; }
            50% { transform: translateX(-50%) scale(1.2); opacity: 1; }
        }
    
        .intro-text {
            text-align: center;
            max-width: 800px;
            margin: 0 auto 4rem;
            font-size: 1.3rem;
            color: #F8F0C6;
            line-height: 1.8;
            font-family: 'Playfair Display', serif;
            text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.5);
        }
    
        .media-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 3rem;
            margin-bottom: 5rem;
        }
    
        .media-card {
            background: linear-gradient(145deg, rgba(44, 36, 27, 0.95), rgba(26, 19, 4, 0.95));
            border-radius: 20px;
            overflow: hidden;
            transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
            box-shadow: 
                0 15px 35px rgba(0, 0, 0, 0.4),
                0 5px 15px rgba(212, 175, 55, 0.1),
                inset 0 1px 0 rgba(212, 175, 55, 0.2);
            border: 1px solid rgba(212, 175, 55, 0.3);
            height: 100%;
            display: flex;
            flex-direction: column;
            position: relative;
        }
    
        .media-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(45deg, transparent, rgba(212, 175, 55, 0.05), transparent);
            opacity: 0;
            transition: opacity 0.5s ease;
            pointer-events: none;
            z-index: 1;
        }
    
        .media-card:hover {
            transform: translateY(-12px) scale(1.03);
            box-shadow: 
                0 25px 50px rgba(0, 0, 0, 0.5),
                0 10px 25px rgba(212, 175, 55, 0.2),
                inset 0 1px 0 rgba(212, 175, 55, 0.3);
        }
    
        .media-card:hover::before {
            opacity: 1;
        }
    
        .media-image {
            height: 240px;
            overflow: hidden;
            position: relative;
        }
    
        .media-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.6s cubic-bezier(0.4, 0, 0.2, 1);
            filter: sepia(10%) saturate(1.1) contrast(1.05);
        }
    
        .image-overlay {
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(45deg, rgba(212, 175, 55, 0.1), transparent, rgba(212, 175, 55, 0.1));
            opacity: 0;
            transition: opacity 0.5s ease;
        }
    
        .media-card:hover .media-image img {
            transform: scale(1.15);
            filter: sepia(15%) saturate(1.2) contrast(1.1);
        }
    
        .media-card:hover .image-overlay {
            opacity: 1;
        }
    
        .media-type {
            position: absolute;
            top: 15px;
            right: 15px;
            background: linear-gradient(45deg, #D4AF37, #F4E4BC);
            color: #1a1304;
            padding: 0.5rem 1rem;
            border-radius: 25px;
            font-size: 0.85rem;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            font-family: 'Cinzel', serif;
            box-shadow: 0 4px 15px rgba(212, 175, 55, 0.4);
            z-index: 2;
        }
    
        .media-content {
            padding: 2.5rem;
            flex-grow: 1;
            display: flex;
            flex-direction: column;
            position: relative;
            z-index: 2;
        }
    
        .media-title {
            font-family: 'Cinzel', serif;
            font-size: 2rem;
            margin-bottom: 1.2rem;
            color: #D4AF37;
            position: relative;
            padding-bottom: 1rem;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
        }
    
        .media-title::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 60px;
            height: 3px;
            background: linear-gradient(to right, #D4AF37, #F4E4BC, transparent);
            border-radius: 2px;
            box-shadow: 0 0 10px rgba(212, 175, 55, 0.5);
        }
    
        .media-year {
            font-weight: 700;
            color: #D4AF37;
            margin-bottom: 1.5rem;
            font-size: 1.2rem;
            display: inline-block;
            padding: 0.6rem 1.5rem;
            background: linear-gradient(45deg, rgba(212, 175, 55, 0.15), rgba(244, 228, 188, 0.1));
            border-radius: 30px;
            border: 2px solid rgba(212, 175, 55, 0.4);
            font-family: 'Cinzel', serif;
            letter-spacing: 2px;
            text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.5);
            box-shadow: inset 0 1px 0 rgba(212, 175, 55, 0.2);
        }
    
        .media-description {
            color: #F8F0C6;
            line-height: 1.8;
            margin-bottom: 2rem;
            flex-grow: 1;
            font-size: 1.05rem;
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.3);
        }
    
        .van-gogh-reference {
            background: linear-gradient(135deg, rgba(212, 175, 55, 0.15), rgba(244, 228, 188, 0.1));
            padding: 1.8rem;
            border-radius: 15px;
            border-left: 5px solid #D4AF37;
            margin-top: auto;
            box-shadow: 
                inset 0 1px 0 rgba(212, 175, 55, 0.2),
                0 4px 15px rgba(0, 0, 0, 0.2);
            position: relative;
            overflow: hidden;
        }
    
        .van-gogh-reference::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(45deg, transparent, rgba(212, 175, 55, 0.05), transparent);
            pointer-events: none;
        }
    
        .reference-title {
            font-family: 'Cinzel', serif;
            color: #D4AF37;
            margin-bottom: 0.8rem;
            font-size: 1.2rem;
            display: flex;
            align-items: center;
            gap: 10px;
            font-weight: 600;
            text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.5);
        }
    
        .reference-title i {
            font-size: 1.3rem;
            filter: drop-shadow(0 0 5px rgba(212, 175, 55, 0.6));
        }
    
        .reference-text {
            color: #F8F0C6;
            line-height: 1.7;
            font-size: 1rem;
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.3);
        }
    
        .quote-section {
            background: linear-gradient(135deg, rgba(212, 175, 55, 0.15), rgba(184, 134, 11, 0.1));
            padding: 4rem 3rem;
            border-radius: 25px;
            text-align: center;
            margin: 5rem 0;
            border: 2px solid rgba(212, 175, 55, 0.4);
            box-shadow: 
                0 15px 35px rgba(0, 0, 0, 0.3),
                inset 0 1px 0 rgba(212, 175, 55, 0.2);
            position: relative;
            overflow: hidden;
        }
    
        .quote-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: radial-gradient(circle at center, rgba(212, 175, 55, 0.1), transparent);
            pointer-events: none;
        }
    
        .quote-decoration {
            color: #D4AF37;
            font-size: 2rem;
            margin: 1rem 0;
            text-shadow: 0 0 20px rgba(212, 175, 55, 0.8);
            animation: sparkle 4s ease-in-out infinite;
        }
    
        .quote-text {
            font-size: 2.2rem;
            font-style: italic;
            color: #F8F0C6;
            margin: 2rem auto;
            line-height: 1.6;
            max-width: 900px;
            font-family: 'Playfair Display', serif;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
            position: relative;
            z-index: 1;
        }
    
        .quote-author {
            font-family: 'Cinzel', serif;
            color: #D4AF37;
            font-size: 1.4rem;
            font-weight: 600;
            text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.5);
            position: relative;
            z-index: 1;
        }
    
        /* Efecto de partículas doradas */
        .gold-particles {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 1;
        }
    
        .particle {
            position: absolute;
            background: radial-gradient(circle, #D4AF37, transparent);
            border-radius: 50%;
            opacity: 0;
            animation: float 12s infinite linear;
        }
    
        @keyframes float {
            0% {
                transform: translateY(100vh) translateX(0) rotate(0deg);
                opacity: 0;
            }
            10% {
                opacity: 0.8;
            }
            90% {
                opacity: 0.3;
            }
            100% {
                transform: translateY(-100vh) translateX(200px) rotate(360deg);
                opacity: 0;
            }
        }
    
        /* Responsive */
        @media screen and (max-width: 768px) {
            .references-container {
                padding: 3rem 1.5rem;
            }
            
            .section-title {
                font-size: 2.8rem;
                margin-bottom: 3rem;
            }
            
            .media-grid {
                grid-template-columns: 1fr;
                gap: 2.5rem;
            }
            
            .media-content {
                padding: 2rem;
            }
            
            .media-title {
                font-size: 1.7rem;
            }
            
            .quote-text {
                font-size: 1.8rem;
            }
            
            .quote-section {
                padding: 3rem 2rem;
            }
        }
    
        @media screen and (max-width: 480px) {
            .references-container {
                padding: 2rem 1rem;
            }
            
            .section-title {
                font-size: 2.2rem;
                margin-bottom: 2.5rem;
            }
            
            .intro-text {
                font-size: 1.1rem;
                margin-bottom: 3rem;
            }
            
            .media-image {
                height: 200px;
            }
            
            .media-content {
                padding: 1.5rem;
            }
            
            .quote-text {
                font-size: 1.5rem;
            }
            
            .quote-section {
                padding: 2.5rem 1.5rem;
            }
        }
    </style>
