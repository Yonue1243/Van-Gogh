    <footer class="footer">
        
        <div class="footer-content">
            <div class="footer-section">
                <h3 class="footer-heading">Sobre Van Gogh</h3>
                <p class="footer-text">
                    Vincent van Gogh (1853-1890) fue un pintor neerlandés, figura central del postimpresionismo. Su obra, caracterizada por colores vibrantes y pinceladas expresivas, influyó profundamente en el arte del siglo XX.
                </p>
                <p class="signature">"He puesto mi corazón y mi alma en mi trabajo, y he perdido mi mente en el proceso"</p>
            </div>
            

            
            <div class="footer-section">
                <h3 class="footer-heading">Museos Relacionados</h3>
                <ul class="footer-links">
                    <li>
                        <a href="https://www.vangoghmuseum.nl" target="_blank" rel="noopener noreferrer">
                            <i class="fas fa-museum"></i> Museo Van Gogh (Ámsterdam)
                        </a>
                    </li>
                    <li>
                        <a href="https://www.musee-orsay.fr" target="_blank" rel="noopener noreferrer">
                            <i class="fas fa-museum"></i> Museo de Orsay (París)
                        </a>
                    </li>
                    <li>
                        <a href="https://krollermuller.nl" target="_blank" rel="noopener noreferrer">
                            <i class="fas fa-museum"></i> Museo Kröller-Müller (Países Bajos)
                        </a>
                    </li>
                    <li>
                        <a href="https://www.moma.org" target="_blank" rel="noopener noreferrer">
                            <i class="fas fa-museum"></i> MoMA (Nueva York)
                        </a>
                    </li>
                </ul>
            </div>
            
            <div class="footer-section">
                <h3 class="footer-heading">Suscríbete</h3>
                <p class="footer-text">
                    Recibe noticias sobre exposiciones, eventos y novedades relacionadas con Van Gogh.
                </p>
                <div class="footer-newsletter">
                    <div class="newsletter-form">
                        <input type="email" class="newsletter-input" placeholder="Tu correo electrónico">
                        <button class="newsletter-btn"><i class="fas fa-paper-plane"></i></button>
                    </div>
                    <div class="social-icons">
                        <a href="#"><i class="fab fa-facebook-f"></i></a>
                        <a href="#"><i class="fab fa-twitter"></i></a>
                        <a href="#"><i class="fab fa-instagram"></i></a>
                        <a href="#"><i class="fab fa-pinterest"></i></a>
                    </div>
                </div>
            </div>
        </div>
        
        <div class="footer-bottom">
            <p class="copyright">© 2025 Van Gogh en la Cultura Popular. Todos los derechos reservados.</p>
            <div class="footer-bottom-links">
                <a href="#">Política de Privacidad</a>
                <a href="#">Términos de Uso</a>
                <a href="#">Cookies</a>
            </div>
        </div>
    </footer>


        <style>
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
            flex: 1;
        }

        .footer {
            background: linear-gradient(to bottom, rgba(26, 19, 0, 0.95), rgba(20, 15, 0, 0.98));
            border-top: 2px solid var(--primary-color);
            padding: 4rem 0 2rem;
            position: relative;
            overflow: hidden;
        }

        .footer::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 1px;
            background: linear-gradient(to right, transparent, var(--primary-color), transparent);
        }

        .footer-content {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 3rem;
        }

        .footer-section {
            display: flex;
            flex-direction: column;
        }

        .footer-heading {
            font-family: 'Playfair Display', serif;
            color: var(--light-gold);
            font-size: 1.5rem;
            margin-bottom: 1.5rem;
            position: relative;
            padding-bottom: 0.5rem;
        }

        .footer-heading::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 50px;
            height: 2px;
            background: var(--primary-color);
        }

        .footer-links {
            list-style: none;
        }

        .footer-links li {
            margin-bottom: 0.8rem;
        }

        .footer-links a {
            color: var(--text-light);
            text-decoration: none;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .footer-links a:hover {
            color: var(--primary-color);
            transform: translateX(5px);
        }

        .footer-links i {
            color: var(--primary-color);
            font-size: 0.9rem;
        }

        .footer-text {
            color: #ccc;
            line-height: 1.6;
            margin-bottom: 1.5rem;
        }

        .footer-newsletter {
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .newsletter-form {
            display: flex;
            gap: 0.5rem;
        }

        .newsletter-input {
            flex: 1;
            padding: 0.8rem 1rem;
            border: 1px solid rgba(212, 175, 55, 0.3);
            border-radius: 4px;
            background: rgba(44, 36, 27, 0.5);
            color: var(--text-light);
            font-family: 'Raleway', sans-serif;
        }

        .newsletter-input:focus {
            outline: none;
            border-color: var(--primary-color);
        }

        .newsletter-btn {
            padding: 0.8rem 1.2rem;
            background: var(--primary-color);
            color: var(--dark-bg);
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-weight: 600;
            transition: all 0.3s ease;
        }

        .newsletter-btn:hover {
            background: var(--light-gold);
        }

        .social-icons {
            display: flex;
            gap: 1rem;
            margin-top: 1.5rem;
        }

        .social-icons a {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: rgba(212, 175, 55, 0.1);
            color: var(--primary-color);
            transition: all 0.3s ease;
        }

        .social-icons a:hover {
            background: var(--primary-color);
            color: var(--dark-bg);
            transform: translateY(-3px);
        }

        .footer-bottom {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem 2rem 0;
            border-top: 1px solid rgba(212, 175, 55, 0.2);
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 1rem;
        }

        .copyright {
            color: #999;
            font-size: 0.9rem;
        }

        .footer-bottom-links {
            display: flex;
            gap: 1.5rem;
        }

        .footer-bottom-links a {
            color: #999;
            text-decoration: none;
            font-size: 0.9rem;
            transition: color 0.3s ease;
        }

        .footer-bottom-links a:hover {
            color: var(--primary-color);
        }

        .signature {
            color: var(--primary-color);
            font-style: italic;
            margin-top: 0.5rem;
        }

        .gold-particles {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 0;
        }

        .particle {
            position: absolute;
            background: radial-gradient(circle, var(--light-gold), transparent);
            border-radius: 50%;
            opacity: 0;
            animation: float 8s infinite linear;
        }

        @keyframes float {
            0% {
                transform: translateY(0) translateX(0) rotate(0deg);
                opacity: 0;
            }
            10% {
                opacity: 0.4;
            }
            90% {
                opacity: 0.2;
            }
            100% {
                transform: translateY(-50px) translateX(20px) rotate(360deg);
                opacity: 0;
            }
        }

        @media screen and (max-width: 768px) {
            .footer-content {
                grid-template-columns: 1fr;
                gap: 2rem;
            }
            
            .newsletter-form {
                flex-direction: column;
            }
            
            .footer-bottom {
                flex-direction: column;
                text-align: center;
            }
            
            .footer-bottom-links {
                justify-content: center;
            }
        }
    </style>